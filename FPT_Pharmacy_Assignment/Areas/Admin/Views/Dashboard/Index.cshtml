@model List<FPT_Pharmacy_Assignment.Areas.Admin.Models.Order>
@{
	ViewData["Title"] = "Admin Panel";
}

<div class="container-fluid">
	<!--  Row 1 -->
	<!-- Add Analysis Report for Products -->
	<div class="row mt-5">
		<div class="col-lg-6">
			<div class="card">
				<div class="card-body">
					<h5 class="card-title">Analysis Report for Products</h5>
					<table class="table">
						<thead>
							<tr>
								<th>Product Name</th>
								<th>Total Sales Quantity</th>
								<th>Total Income Generated</th>
							</tr>
						</thead>
						<tbody>
							@foreach (var product in Model)
							{
								
							}
						</tbody>
					</table>
				</div>
			</div>
		</div>
	</div>

	<!-- Chart Income -->
	<div class="row mt-5">
		<div class="col-lg-12">
			<div class="card">
				<div class="card-body">
					<h5 class="card-title">Chart Income</h5>
					<!-- Add chart to visualize income over time -->
					<!-- Example: Line chart showing income trend over months or years -->
				</div>
			</div>
		</div>
	</div>

	<div class="row">
		<!-- Recent Transactions -->
		<div class="col-lg-4 d-flex align-items-stretch">
			<div class="card w-100">
				<div class="card-body p-4">
					<div class="mb-4">
						<h5 class="card-title fw-semibold">Recent Transactions</h5>
					</div>
					<ul class="timeline-widget mb-0 position-relative mb-n5">
						@foreach (var order in Model)
						{
							<li class="timeline-item d-flex position-relative overflow-hidden">
								<!-- Display transaction details -->
								<div class="timeline-time text-dark flex-shrink-0 text-end">@order.CreatedAt.ToString("HH:mm tt")</div>
								<div class="timeline-badge-wrap d-flex flex-column align-items-center">
									<span class="timeline-badge border-2 border border-primary flex-shrink-0 my-8"></span>
									<span class="timeline-badge-border d-block flex-shrink-0"></span>
								</div>
								<div class="timeline-desc fs-3 text-dark mt-n1">Payment received from user @order.UserId of $@order.TotalPrice</div>
							</li>
						}
					</ul>
				</div>
			</div>
		</div>
		<!-- Display Recent Transactions in a Table -->
		<div class="col-lg-8 d-flex align-items-stretch">
			<div class="card w-100">
				<div class="card-body p-4">
					<h5 class="card-title fw-semibold mb-4">Recent Transactions</h5>
					<div class="table-responsive">
						<table class="table text-nowrap mb-0 align-middle">
							<thead class="text-dark fs-4">
								<tr>
									<th class="border-bottom-0">
										<h6 class="fw-semibold mb-0">Id</h6>
									</th>
									<th class="border-bottom-0">
										<h6 class="fw-semibold mb-0">UserId</h6>
									</th>
									<th class="border-bottom-0">
										<h6 class="fw-semibold mb-0">Status</h6>
									</th>
									<th class="border-bottom-0">
										<h6 class="fw-semibold mb-0">Total Price</h6>
									</th>
									<th class="border-bottom-0">
										<h6 class="fw-semibold mb-0">Created At</h6>
									</th>
								</tr>
							</thead>
							<tbody>
								<!-- Display your recent transactions data in a table format -->
								@foreach (var order in Model)
								{
									<tr>
										<td>@order.OrderId</td>
										<td>@order.UserId</td>
										<td>@order.Status</td>
										<td>$@order.TotalPrice</td>
										<td>@order.CreatedAt.ToString("MM/dd/yyyy")</td>
									</tr>
								}
							</tbody>
						</table>
					</div>
				</div>
			</div>
		</div>
	</div>
	<footer class="py-6 px-6 text-center">
		<span>&copy; @DateTime.Now.Year Admin Panel. All rights reserved.</span>
	</footer>
</div>
